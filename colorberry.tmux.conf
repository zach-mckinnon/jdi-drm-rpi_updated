# ColorBerry / Beepy tmux status line
set -g status on
set -g status-position bottom
set -g status-interval 30
set -g status-left ""
set -g status-right "#(/usr/local/bin/colorberry-battery --percent 2>/dev/null || printf -- '--')%% | %H:%M"
set -g window-status-separator " | "
